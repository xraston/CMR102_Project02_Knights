using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HealthAndDamage : MonoBehaviour
{
    // health and damage variables
    public int attackLightDMG;
    public int attackHeavyDMG;
    public int knightHealthTotal = 100;
    public int knightHealthCurrent;
    public int opponentHealthCurrent;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public void LightAttackDamage()
    {
        // generates the damage on the light attack within a random range
    }

    public void HeavyAttackDamage()
    {
        // generates the damage on the heavy attack within a random range
    }
}
